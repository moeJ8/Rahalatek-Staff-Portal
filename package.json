{
  "name": "tour-management-system",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon api/index.js",
    "start": "node api/index.js",
    "client-install": "cd client && npm install --legacy-peer-deps",
    "client-build": "cd client && npm run build",
    "build": "npm install && npm run client-install && npm run client-build && npx puppeteer browsers install chrome --path /opt/render/.cache/puppeteer",
    "postinstall": "npx puppeteer browsers install chrome --path /opt/render/.cache/puppeteer || npx puppeteer browsers install chrome || node scripts/setup-chrome.js",
    "render-build": "npm install && npm run client-install && npm run client-build && npx puppeteer browsers install chrome --path /opt/render/.cache/puppeteer",
    "verify-paths": "node verify-paths.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.12.2",
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.0",
    "html2canvas": "^1.4.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^8.13.2",
    "mongoose-paginate-v2": "^1.9.1",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.6",
    "puppeteer": "^24.20.0",
    "qrcode": "^1.5.4",
    "redis": "^5.8.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
